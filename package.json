{
  "name": "ghl-automator",
  "version": "1.0.0",
  "description": "GoHighLevel Sub-Account and User Management App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "dev:client": "cd client && npm run dev",
    "electron": "electron electron.js",
    "electron:dev": "cross-env APP_URL=http://localhost:3000 electron electron.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.1",
    "@upstash/redis": "^1.38.0",
    "@vercel/functions": "^2.2.13",
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "openai": "^6.47.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "nodemon": "^3.1.9"
  }
}
